import {StyleSheet} from 'react-native';
import {poppins500} from '../../utils/fonts/poppins';

export const styles = StyleSheet.create({
  tabBarLabelStyle: {
    fontSize: 12,
    fontFamily: poppins500,
    marginBottom: 2,
  },
});
